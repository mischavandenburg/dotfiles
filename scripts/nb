#!/bin/bash
# nb opens a new pane and runs newsboat in it. I want to read from a centered column in my screen.
tmux split-pane -h \; resize-pane -x 150\; send -t 2 "newsboat -r" Enter\; send -t 1 "clear" Enter
